version: '3.8'

services:
  etf-portfolio-tool:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./cache:/app/cache
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
